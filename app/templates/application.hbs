<div class="page-wrapper markdown-body">
  <div class="header-nav">
    <h1 class="header">{{link-to "Teamtailor code styleguide" "application"}}</h1>

    <nav class="language-navigation">
      {{#each model as |language|}}
        {{link-to language.title "language" language class="language-navigation__link"}}
      {{/each}}
    </nav>
  </div>
  {{outlet}}
</div>
